apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: namiview-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: git@github.com:darbuki/namiviewk8s.git
    targetRevision: main
    path: apps/namiview
  destination:
    server: https://kubernetes.default.svc
    namespace: namiview-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true