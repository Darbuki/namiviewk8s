apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-admin
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: grafana-admin-secret
    creationPolicy: Owner
  data:
    # Source SM uses camelCase (legacy from EKS setup); k8s secret keys are kebab to
    # match the Grafana helm chart's `userKey`/`passwordKey` defaults.
    - secretKey: admin-user
      remoteRef:
        key: namiview-prod/grafana
        property: adminUser
    - secretKey: admin-password
      remoteRef:
        key: namiview-prod/grafana
        property: adminPassword
