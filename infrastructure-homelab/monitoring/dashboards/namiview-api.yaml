apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-namiview-api
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  namiview-api.json: |
    {
      "uid": "namiview-api",
      "title": "Namiview · API",
      "tags": ["namiview"],
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "time": {"from": "now-1h", "to": "now"},
      "templating": {
        "list": [
          {
            "name": "namespace",
            "type": "query",
            "datasource": {"type": "prometheus", "uid": "prometheus"},
            "query": "label_values(http_requests_total{handler=~\"/api.*\"}, namespace)",
            "current": {"text": "namiview", "value": "namiview"},
            "refresh": 1,
            "sort": 1
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Request rate by route",
          "type": "timeseries",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8},
          "targets": [
            {"expr": "sum by (handler) (rate(http_requests_total{namespace=\"$namespace\", handler=~\"/api.*\"}[5m]))",
             "legendFormat": "{{handler}}"}
          ],
          "fieldConfig": {"defaults": {"unit": "reqps"}}
        },
        {
          "id": 2,
          "title": "Error rate (4xx/5xx)",
          "type": "timeseries",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8},
          "targets": [
            {"expr": "sum by (status) (rate(http_requests_total{namespace=\"$namespace\", status=~\"4..|5..\"}[5m]))",
             "legendFormat": "{{status}}"}
          ],
          "fieldConfig": {"defaults": {"unit": "reqps"}}
        },
        {
          "id": 3,
          "title": "Latency p50 / p95 / p99",
          "type": "timeseries",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 0, "y": 8, "w": 12, "h": 8},
          "targets": [
            {"expr": "histogram_quantile(0.50, sum by (le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\"}[5m])))",
             "legendFormat": "p50"},
            {"expr": "histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\"}[5m])))",
             "legendFormat": "p95"},
            {"expr": "histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\"}[5m])))",
             "legendFormat": "p99"}
          ],
          "fieldConfig": {"defaults": {"unit": "s"}}
        },
        {
          "id": 4,
          "title": "Latency p95 by route",
          "type": "timeseries",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 12, "y": 8, "w": 12, "h": 8},
          "targets": [
            {"expr": "histogram_quantile(0.95, sum by (handler, le) (rate(http_request_duration_seconds_bucket{namespace=\"$namespace\", handler=~\"/api.*\"}[5m])))",
             "legendFormat": "{{handler}}"}
          ],
          "fieldConfig": {"defaults": {"unit": "s"}}
        },
        {
          "id": 5,
          "title": "HTTP status breakdown",
          "type": "stat",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 0, "y": 16, "w": 8, "h": 5},
          "targets": [
            {"expr": "sum by (status) (rate(http_requests_total{namespace=\"$namespace\"}[5m]))",
             "legendFormat": "{{status}}"}
          ],
          "fieldConfig": {"defaults": {"unit": "reqps"}}
        },
        {
          "id": 6,
          "title": "Images uploaded (selected range)",
          "description": "increase() over the dashboard range. The bare counter this replaced was cumulative since pod start, so it silently meant 'since the last deploy' while sitting next to a time picker.",
          "type": "stat",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 8, "y": 16, "w": 8, "h": 5},
          "targets": [
            {"expr": "sum(increase(namiview_images_uploaded_total{namespace=\"$namespace\"}[$__range]))",
             "legendFormat": "uploaded"}
          ],
          "fieldConfig": {"defaults": {"decimals": 0}}
        },
        {
          "id": 7,
          "title": "Gallery images (total in Mongo)",
          "description": "Absolute count from MongoDB, refreshed every 5 min — not a counter, so it survives restarts. Replaces a namiview_images_saved_total panel that read 0 forever: the current save path (POST /jobs/{id}/save) bypasses services/gallery_service.py where IMAGES_SAVED.inc() lives. See Darbuki/namiview#390.",
          "type": "stat",
          "datasource": {"type": "prometheus", "uid": "prometheus"},
          "gridPos": {"x": 16, "y": 16, "w": 8, "h": 5},
          "targets": [
            {"expr": "max(namiview_gallery_images{namespace=\"$namespace\"})",
             "legendFormat": "gallery images"}
          ],
          "fieldConfig": {"defaults": {"decimals": 0}}
        }
      ]
    }
