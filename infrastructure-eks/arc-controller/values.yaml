# Overrides for gha-runner-scale-set-controller v0.14.0
# Defaults: helm show values oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set-controller --version 0.14.0

flags:
  logLevel: "info"

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
