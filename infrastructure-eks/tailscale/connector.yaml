# Connector — runs a Tailscale-daemon pod that joins the tailnet and
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: namiview-vpc-router
spec:
  hostname: namiview-vpc-router
  subnetRouter:
    advertiseRoutes:
      - 10.0.0.0/16
