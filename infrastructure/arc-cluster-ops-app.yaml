apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arc-cluster-ops
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:darbuki/namiviewk8s.git
    targetRevision: main
    path: infrastructure/arc-cluster-ops
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: arc-runners
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
